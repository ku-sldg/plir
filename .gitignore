# Rocq / Coq build artifacts
*.vo
*.vok
*.vos
*.glob
.*.aux
*.cache
.lia.cache
# coq_makefile dependency files (regenerated by make).
# Makefile and Makefile.conf are tracked so `make` works on a fresh clone.
.*.d
.coqdeps.d

# Generated documentation sites (rebuilt by `make docs` / coqdoc)
/site/
/html/

# Editor / assistant local config
CLAUDE.md
.claude/
.vscode/
