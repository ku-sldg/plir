# plir — project memory

Project-local persistent memory lives in `.claude/memory/`. Each file holds one fact with frontmatter; `.claude/memory/MEMORY.md` is the index. **Read, update, and add memory there — not in the global `~/.claude` location.**

Memory index:

@.claude/memory/MEMORY.md
